class Product {
  constructor (
    id,
    title,
    description,
    code,
    price,
    status,
    stock,
    category,
    thunbnails
  ) {
    this.id = id
    this.title = title
    this.description = description
    this.code = code
    this.price = price
    this.status = status
    this.stock = stock
    this.category = category
    this.thunbnails = thunbnails
  }
}

export default Product
